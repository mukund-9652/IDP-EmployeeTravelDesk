import { ReimbursementRequests } from './reimbursement-requests';

describe('ReimbursementRequests', () => {
  it('should create an instance', () => {
    expect(new ReimbursementRequests()).toBeTruthy();
  });
});
