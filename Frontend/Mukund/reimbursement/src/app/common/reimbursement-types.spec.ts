import { ReimbursementTypes } from './reimbursement-types';

describe('ReimbursementTypes', () => {
  it('should create an instance', () => {
    expect(new ReimbursementTypes()).toBeTruthy();
  });
});
